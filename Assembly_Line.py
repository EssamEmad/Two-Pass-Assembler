class Assembly_Line:

"""A container class to encapsulate all of the information in an assembly line including both
what the programmer enters and what's being used to generate the output as location counter, object
code ...etc
"""

    def __init__(self, current_address, label, mnemonic, operands):
        self.current_address = current_address
        self.label = label
        self.memonic = memonic
        self.operands = operands

    def current_address #location counter
    def label #label or None
    def mnemonic
    def operands #list of operands
